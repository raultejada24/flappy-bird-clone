# Flappy Bird Clone

A browser game built with JavaScript Canvas, collision detection and game loops.

## Run

```bash
npm start
```

Open `http://localhost:3000`. Requires Node.js 18 or newer.

## Controls

- `Space`, click or tap to fly.
- `P` to pause.
