{
  "name": "flappy-bird-clone",
  "version": "1.0.0",
  "private": true,
  "description": "A browser-based flying game built with JavaScript Canvas.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check game.js"
  }
}
